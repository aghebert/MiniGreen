# MiniGreen - Frontend

## Configuration

In order to use MongoDB, make sure these database settings are added to the `setup.py`

```
DATABASES = {
   'default' : {
      'ENGINE' : 'djongo',
      'NAME' : '<database_name>'
   }
}
```

## Thoughts
